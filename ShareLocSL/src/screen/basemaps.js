
import React from 'react';
import BaseScreen from './basescreen';
import {

  BackHandler
} from 'react-native';

export default class BaseMapsScreen extends BaseScreen {
    constructor(props) {
        super(props);
    }
}